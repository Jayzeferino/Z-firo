# Email Sequence

When the user wants to create or optimize an email sequence, drip campaign, automated email flow, or lifecycle email program. Also use when the user mentions "email sequence," "drip campaign," "nurture sequence," "onboarding emails," "welcome sequence," "re-engagement emails," "email automation," or "lifecycle emails." For in-app onboarding, see onboarding-cro.

## What it is
An agent-agnostic marketing skill focused on **email sequence**.

## When to use it
Use this skill in the following scenarios:
- When the user requests: *"email sequence"*
- When the user requests: *"drip campaign"*
- When the user requests: *"nurture sequence"*
- When the user requests: *"welcome sequence"*
- When the user requests: *"onboarding emails"*
- When the user requests: *"re-engagement emails"*
- When the user requests: *"email automation"*
- When the user requests: *"lifecycle emails"*

## How to use
1. Trigger this skill by asking the marketing agent for tasks related to **email sequence**.
2. The agent will read `SKILL.md` to guide its execution workflow.
3. Ensure you provide relevant product positioning and customer context, which can be generated using the `clarify-product` skill.

## Related Skills
- `email-marketing`
- `copywriting`
- `onboarding-cro`
