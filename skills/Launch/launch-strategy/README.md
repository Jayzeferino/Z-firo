# Launch Strategy

When the user wants to plan a product launch, feature announcement, or release strategy. Also use when the user mentions 'launch,' 'Product Hunt,' 'feature release,' 'announcement,' 'go-to-market,' 'beta launch,' 'early access,' 'waitlist,' or 'product update.' This skill covers phased launches, channel strategy, and ongoing launch momentum.

## What it is
An agent-agnostic marketing skill focused on **launch strategy**.

## When to use it
Use this skill in the following scenarios:
- When the user requests: *"launch"*
- When the user requests: *"Product Hunt"*
- When the user requests: *"feature release"*
- When the user requests: *"announcement"*
- When the user requests: *"go-to-market"*
- When the user requests: *"beta launch"*
- When the user requests: *"early access"*
- When the user requests: *"waitlist"*
- When the user requests: *"product update"*

## How to use
1. Trigger this skill by asking the marketing agent for tasks related to **launch strategy**.
2. The agent will read `SKILL.md` to guide its execution workflow.
3. Ensure you provide relevant product positioning and customer context, which can be generated using the `clarify-product` skill.

## Related Skills
- `content-strategy`
- `social-media`
- `email-sequence`
