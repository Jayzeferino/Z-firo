# Ab Testing

When the user wants to plan, design, or implement an A/B test or experiment. Also use when the user mentions "A/B test," "split test," "experiment," "test this change," "variant copy," "multivariate test," or "hypothesis." For tracking implementation, see analytics-tracking.

## What it is
An agent-agnostic marketing skill focused on **ab testing**.

## When to use it
Use this skill in the following scenarios:
- When the user requests: *"A/B test"*
- When the user requests: *"split test"*
- When the user requests: *"experiment"*
- When the user requests: *"test this change"*
- When the user requests: *"variant copy"*
- When the user requests: *"multivariate test"*
- When the user requests: *"hypothesis"*
- When the user requests: *"statistical significance"*

## How to use
1. Trigger this skill by asking the marketing agent for tasks related to **ab testing**.
2. The agent will read `SKILL.md` to guide its execution workflow.
3. Ensure you provide relevant product positioning and customer context, which can be generated using the `clarify-product` skill.

## Related Skills
- `page-cro`
- `analytics-attribution`
