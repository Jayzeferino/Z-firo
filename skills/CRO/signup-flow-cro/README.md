# Signup Flow Cro

When the user wants to optimize signup, registration, account creation, or trial activation flows. Also use when the user mentions "signup conversions," "registration friction," "signup form optimization," "free trial signup," "reduce signup dropoff," or "account creation flow." For post-signup onboarding, see onboarding-cro. For lead capture forms (not account creation), see form-cro.

## What it is
An agent-agnostic marketing skill focused on **signup flow cro**.

## When to use it
Use this skill in the following scenarios:
- When the user requests: *"signup"*
- When the user requests: *"registration"*
- When the user requests: *"account creation"*
- When the user requests: *"trial signup"*
- When the user requests: *"signup form"*
- When the user requests: *"signup dropoff"*
- When the user requests: *"registration friction"*

## How to use
1. Trigger this skill by asking the marketing agent for tasks related to **signup flow cro**.
2. The agent will read `SKILL.md` to guide its execution workflow.
3. Ensure you provide relevant product positioning and customer context, which can be generated using the `clarify-product` skill.

## Related Skills
- `form-cro`
- `onboarding-cro`
