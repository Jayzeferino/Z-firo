# Popup Cro

When the user wants to create or optimize popups, modals, overlays, slide-ins, or banners for conversion purposes. Also use when the user mentions "exit intent," "popup conversions," "modal optimization," "lead capture popup," "email popup," "announcement banner," or "overlay." For forms outside of popups, see form-cro. For general page conversion optimization, see page-cro.

## What it is
An agent-agnostic marketing skill focused on **popup cro**.

## When to use it
Use this skill in the following scenarios:
- When the user requests: *"popup"*
- When the user requests: *"modal"*
- When the user requests: *"overlay"*
- When the user requests: *"exit intent"*
- When the user requests: *"email popup"*
- When the user requests: *"lead capture popup"*
- When the user requests: *"announcement banner"*
- When the user requests: *"slide-in"*

## How to use
1. Trigger this skill by asking the marketing agent for tasks related to **popup cro**.
2. The agent will read `SKILL.md` to guide its execution workflow.
3. Ensure you provide relevant product positioning and customer context, which can be generated using the `clarify-product` skill.

## Related Skills
- `page-cro`
- `form-cro`
- `copywriting`
