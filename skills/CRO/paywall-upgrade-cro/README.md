# Paywall Upgrade Cro

When the user wants to create or optimize in-app paywalls, upgrade screens, upsell modals, or feature gates. Also use when the user mentions "paywall," "upgrade screen," "upgrade modal," "upsell," "feature gate," "convert free to paid," "freemium conversion," "trial expiration screen," "limit reached screen," "plan upgrade prompt," or "in-app pricing." Distinct from public pricing pages (see page-cro) — this skill focuses on in-product upgrade moments where the user has already experienced value.

## What it is
An agent-agnostic marketing skill focused on **paywall upgrade cro**.

## When to use it
Use this skill in the following scenarios:
- When the user requests: *"paywall"*
- When the user requests: *"upgrade screen"*
- When the user requests: *"upsell modal"*
- When the user requests: *"feature gate"*
- When the user requests: *"freemium conversion"*
- When the user requests: *"trial expiration"*
- When the user requests: *"upgrade prompt"*
- When the user requests: *"in-app pricing"*

## How to use
1. Trigger this skill by asking the marketing agent for tasks related to **paywall upgrade cro**.
2. The agent will read `SKILL.md` to guide its execution workflow.
3. Ensure you provide relevant product positioning and customer context, which can be generated using the `clarify-product` skill.

## Related Skills
- `pricing-strategy`
- `page-cro`
- `copywriting`
