# Form Cro

When the user wants to optimize any form that is NOT signup/registration — including lead capture forms, contact forms, demo request forms, application forms, survey forms, or checkout forms. Also use when the user mentions "form optimization," "lead form conversions," "form friction," "form fields," "form completion rate," or "contact form." For signup/registration forms, see signup-flow-cro. For popups containing forms, see popup-cro.

## What it is
An agent-agnostic marketing skill focused on **form cro**.

## When to use it
Use this skill in the following scenarios:
- When the user requests: *"form optimization"*
- When the user requests: *"lead form"*
- When the user requests: *"contact form"*
- When the user requests: *"demo request"*
- When the user requests: *"form fields"*
- When the user requests: *"form completion rate"*
- When the user requests: *"form friction"*
- When the user requests: *"form conversions"*

## How to use
1. Trigger this skill by asking the marketing agent for tasks related to **form cro**.
2. The agent will read `SKILL.md` to guide its execution workflow.
3. Ensure you provide relevant product positioning and customer context, which can be generated using the `clarify-product` skill.

## Related Skills
- `page-cro`
- `signup-flow-cro`
- `popup-cro`
