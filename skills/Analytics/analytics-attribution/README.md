# Analytics Attribution

Performance measurement, attribution modeling, and marketing ROI analysis. Use when setting up tracking, analyzing campaign performance, building attribution models, or creating marketing reports.

## What it is
An agent-agnostic marketing skill focused on **analytics attribution**.

## When to use it
Use this skill in the following scenarios:
- When the user requests: *"analytics"*
- When the user requests: *"attribution"*
- When the user requests: *"tracking"*
- When the user requests: *"ROI"*
- When the user requests: *"CAC"*
- When the user requests: *"LTV"*
- When the user requests: *"dashboard"*
- When the user requests: *"reporting"*
- When the user requests: *"conversion tracking"*
- When the user requests: *"marketing metrics"*

## How to use
1. Trigger this skill by asking the marketing agent for tasks related to **analytics attribution**.
2. The agent will read `SKILL.md` to guide its execution workflow.
3. Ensure you provide relevant product positioning and customer context, which can be generated using the `clarify-product` skill.

## Related Skills
- `ab-testing`
- `paid-advertising`
