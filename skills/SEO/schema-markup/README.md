# Schema Markup

When the user wants to add, fix, or optimize schema markup and structured data on their site. Also use when the user mentions "schema markup," "structured data," "JSON-LD," "rich snippets," "schema.org," "FAQ schema," "product schema," "review schema," or "breadcrumb schema." For broader SEO issues, see seo-audit.

## What it is
An agent-agnostic marketing skill focused on **schema markup**.

## When to use it
Use this skill in the following scenarios:
- When the user requests: *"schema"*
- When the user requests: *"structured data"*
- When the user requests: *"JSON-LD"*
- When the user requests: *"rich snippets"*
- When the user requests: *"schema.org"*
- When the user requests: *"FAQ schema"*
- When the user requests: *"product schema"*
- When the user requests: *"review schema"*

## How to use
1. Trigger this skill by asking the marketing agent for tasks related to **schema markup**.
2. The agent will read `SKILL.md` to guide its execution workflow.
3. Ensure you provide relevant product positioning and customer context, which can be generated using the `clarify-product` skill.

## Related Skills
- `seo-mastery`
- `programmatic-seo`
