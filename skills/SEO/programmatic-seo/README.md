# Programmatic Seo

When the user wants to create SEO-driven pages at scale using templates and data. Also use when the user mentions "programmatic SEO," "template pages," "pages at scale," "directory pages," "location pages," "[keyword] + [city] pages," "comparison pages," "integration pages," or "building many pages for SEO." For auditing existing SEO issues, see seo-audit.

## What it is
An agent-agnostic marketing skill focused on **programmatic seo**.

## When to use it
Use this skill in the following scenarios:
- When the user requests: *"programmatic SEO"*
- When the user requests: *"template pages"*
- When the user requests: *"pages at scale"*
- When the user requests: *"directory pages"*
- When the user requests: *"location pages"*
- When the user requests: *"comparison pages"*
- When the user requests: *"integration pages"*

## How to use
1. Trigger this skill by asking the marketing agent for tasks related to **programmatic seo**.
2. The agent will read `SKILL.md` to guide its execution workflow.
3. Ensure you provide relevant product positioning and customer context, which can be generated using the `clarify-product` skill.

## Related Skills
- `seo-mastery`
- `schema-markup`
- `competitor-alternatives`
